---
meta:
  environment: NAME
  dns_root: microbosh
  network_name: s3ninja1
  security_groups:
    - s3ninja

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: s3ninja
    version: latest
